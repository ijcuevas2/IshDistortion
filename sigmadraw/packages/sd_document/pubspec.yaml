name: sd_document
description: "SVG DOM document model: XML (de)serialization with private-namespace round-trip."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.2

resolution: workspace

dependencies:
  collection: ^1.19.1
  meta: ^1.19.0
  xml: ^7.0.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
