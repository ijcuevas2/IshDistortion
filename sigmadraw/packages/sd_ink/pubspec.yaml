name: sd_ink
description: "Stroke model, pressure-to-width curves, stabilizers, and outline generation for pen ink."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.2

resolution: workspace

dependencies:
  meta: ^1.15.0
  sd_document:
    path: ../sd_document

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
