name: sigmadraw
description: "SigmaDraw: a semantically-aware DSP diagramming editor."
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.13.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sd_document:
    path: ../../packages/sd_document
  sd_render:
    path: ../../packages/sd_render
  sd_stencils:
    path: ../../packages/sd_stencils
  sd_ui:
    path: ../../packages/sd_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
