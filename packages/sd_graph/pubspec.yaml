name: sd_graph
description: "Semantic signal-flow graph: typed ports, directed edges, validation, and analysis."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.2

resolution: workspace

dependencies:
  collection: ^1.19.1
  meta: ^1.19.0
  sd_document:
    path: ../sd_document

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
