name: sd_input
description: "Pointer/pen input pipeline: device classification, palm rejection, platform channels."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.2
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
