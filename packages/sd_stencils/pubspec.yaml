name: sd_stencils
description: "DSP symbol/stencil library: geometry generators, ports, parameters, and semantics."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.2

resolution: workspace

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
